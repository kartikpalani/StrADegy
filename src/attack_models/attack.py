class Attack:

	def __init__(self, graph, source, model):
		self.graph = graph
		self.source = source
		self.model = model

	def run(self):
		compromised_nodes = []

		if self.model.name == 'linear_threshold':


		return compromised_nodes


